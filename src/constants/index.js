export const method = {
  GET: 'GET',
  POST: 'POST',
  PATCH: 'PATCH',
};

export const routes = {
  main: '/',
  mobile: '/mobile',
  profile: '/profile',
  auth: '/auth',
}

export const apiRoutes = {
  changePassword: '/api/user/change-password',
  stripeSession: '/api/create-stripe-session',
  comments: '/api/comments',
}
